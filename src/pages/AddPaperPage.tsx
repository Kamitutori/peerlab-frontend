import PaperPageForm from '../components/PaperPageForm.tsx';

// Importing the PaperPageForm component, which is a form to add and edit a paper.
export default function AddPaperPage() {
    return (
        // Returning the PaperPageForm component as part of the UI of the AddPaperPage component.
        <PaperPageForm
        />
    );
}